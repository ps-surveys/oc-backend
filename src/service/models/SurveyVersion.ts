export const TYPE_VERSION = "survey_version";

export class SurveyVersion {
    idVersion: number;
    idFormat: number;
    dateCreated: Date;
    codVersion: string;
    stateVersion: boolean;
    version: number;
}