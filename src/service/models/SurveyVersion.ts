export const TYPE_VERSION = "act_version";

export class ActVersion {
    idVersion: number;
    idFormat: number;
    dateCreated: Date;
    codVersion: string;
    stateVersion: boolean;
    version: number;
}